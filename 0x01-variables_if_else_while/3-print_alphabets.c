#include <stdio.h>

/**
 * main - prints alpha in lower and upper case
 * Return: Always 0
 */
int main(void)
{
	char alph:

	for (alph = "a"; alph <= 'z'; alph++)
		putchar (alph);
	for (alph = 'A'; alph <= 'Z'; alp++)
		putchar(alpha);
	putchar('\n');
	return (0);
}

