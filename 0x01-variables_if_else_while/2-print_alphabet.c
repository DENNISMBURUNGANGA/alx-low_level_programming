#include <stdio.h>

/**
 * main - prints alpha in lower casd
 * Return: Always 0
 */
int main(void)
{
	char ld;

	for (ld = 'a'; ld <= 'z'; ld++)
	{
		putchar (ld);
	}
	putchar ('\n');
	return (0);
}
